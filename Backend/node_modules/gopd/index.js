'use strict';

/** @type {import('.')} */
var $gOPD;
try {
  $gOPD = require('./gOPD');
} catch (e) {
  // Try alternative case variations for Linux
  try {
    const files = require('fs').readdirSync('C:/Project/Medi_connect-app/Backend/node_modules/gopd');
    const gopdFile = files.find(f => f.toLowerCase() === 'gopd.js');
    if (gopdFile) {
      $gOPD = require('./' + gopdFile);
    } else {
      throw new Error('gOPD.js not found');
    }
  } catch (e2) {
    $gOPD = null;
  }
}

if ($gOPD) {
	try {
		$gOPD([], 'length');
	} catch (e) {
		// IE 8 has a broken gOPD
		$gOPD = null;
	}
}

module.exports = $gOPD;
